# 总体目标
参照gotohttp和rustdesk做一款通过浏览器远程控制客户端的工具

# 浏览器页面功能
1. 可以输入客户端ID和客户端验证码来远程控制客户端桌面
2. 点击断开连接，断开和客户端的连接，同时客户端停止截屏操作

# 服务端功能
1. 可以作为STUN服务器实现NAT穿透
2. 可以作为中继服务器只负责转发数据包
3. 可以通过客户端MAC地址生成固定客户端ID

# 客户端功能
1. 客户端第一次启动时会请求服务端颁发客户端ID
2. 客户端有个client.conf配置文件
3. client.conf配置文件包含服务端地址，用于建立和服务端的websocket连接
4. 客户端第一次启动时会自动生成一个验证码，并存储在client.conf中